example:
  property: "I AM THE DEFAULT"
management:
  endpoints:
    web:
      exposure:
        include: health, refresh, beans, env
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
